<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="returnig">
	
		
		<select id="selectAll" resultType="returning.ReturningVo" parameterType="returning.ReturningVo">
			SELECT * FROM returning
		</select>
	
		<insert id="insert" parameterType="returning.ReturningVo">
			INSERT INTO returning (returning_no, returning_reason_no, returning_category, b_no, ps_no, rd_no, refund_no, returning_amount ) 
			VALUES( #{returning_no}, #{returning_reason_no},#{returning_category},#{b_no},#{ps_no},#{ rd_no},#{refund_no},#{returning_amount} )
		</insert>
		
	</mapper>