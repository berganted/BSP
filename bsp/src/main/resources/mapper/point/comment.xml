<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="point">

	<select id="selectAll" resultType="util.CommonVo" parameterType="util.CommonVo">
		SELECT *, (SELECT m_name FROM user WHERE m_no=point.m_no) AS name FROM point
		WHERE m_no=#{m_no}
		ORDER BY $p_regdate DESC
		LIMIT ${strIdx},${pageRow}
	</select>
	
	<select id="count" resultType="int" parameterType="util.CommonVo">
		SELECT COUNT(*) FROM comment
		WHERE q_no=#{q_no} AND c_tablename=#{c_tablename}
	</select>
	
	<insert id="insert" parameterType="util.CommonVo">
		INSERT INTO comment (c_content, c_regdate, q_no, m_no, c_tablename)
		VALUES (#{c_content}, NOW(), #{q_no}, #{m_no}, #{c_tablename})
	</insert>
	
	<delete id="delete" parameterType="util.CommonVo">
		DELETE FROM comment WHERE c_no=#{c_no}
	</delete>
	
</mapper>